# Sample Configurations

Vendor-specific sample configs land here alongside each vendor's parser
(Phases 2-5), paired with "golden output" expected PAN-OS CLI files under
`tests/samples/` for regression testing.

Planned:
- `fortigate_sample.conf` (Phase 2)
- `checkpoint_sample.txt` (Phase 3)
- `cisco_asa_sample.cfg` (Phase 4)
- `sophos_xg_sample.export` (Phase 5)
